import React from "react"
import { View } from "react-native"
import { Button, Text } from "react-native-elements"
// redux test
const ResetPassword = () => {
  return (
    <View>
      <Text h1> ResetPassword</Text>
      <Button title="plus" onPress={() => {}} />
      <Button title="Minus" />
    </View>
  )
}

export default ResetPassword
