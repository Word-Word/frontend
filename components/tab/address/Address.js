import React from "react"
import { View } from "react-native"
import { Button, Text } from "react-native-elements"
// redux test
const AddressComponent = () => {
  return (
    <View>
      <Text h1> Address</Text>
      <Button title="plus" onPress={() => {}} />
      <Button title="Minus" />
    </View>
  )
}

export default AddressComponent
